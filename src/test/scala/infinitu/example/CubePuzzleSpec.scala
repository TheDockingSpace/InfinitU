package infinitu.example

import org.specs2.matcher.ShouldMatchers
import org.specs2.mutable.Specification

object CubePuzzleSpec extends Specification with ShouldMatchers {
  
  "should create tests" in {
    true must beTrue
  }

}